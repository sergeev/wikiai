# Сборка для Linux
``` bash
pyinstaller --onefile --name="wikipedia-ai" main.py
```
# Или с иконкой
``` bash
pyinstaller --onefile --icon=icon.png --name="wikipedia-ai" main.py
```
# Сделать исполняемым
``` bash
chmod +x dist/wikipedia-ai
```
